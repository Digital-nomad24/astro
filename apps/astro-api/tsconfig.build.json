{
  "extends": "./tsconfig.json",
  "//": "scripts/ are tsx-run developer tools (EXPLAIN plans, probes) — linted and typechecked, but never shipped in the image.",
  "exclude": ["node_modules", "test", "dist", "scripts", "**/*spec.ts"]
}

{
  "name": "astro-service-errors",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/astro-service/server/errors/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:errors"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/astro-service/server/errors",
        "command": "tsc -p tsconfig.lib.json"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/astro-service/server/errors",
        "command": "tsc -p tsconfig.lib.json --watch --preserveWatchOutput"
      }
    }
  }
}
